var current_age = 10; 
var max_age = 20; 
var years_to_max = max_age - current_age; 
var amount_per_day = 5; 
var amount_total = years_to_max * 365 * amount_per_day; 

console.log("You will need",amount_total,"to last you until the age of",max_age);

//made a change on github 
//some other change 